NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api
NEXT_PUBLIC_TENANT_SLUG=yourcompany
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
# MapBox access token — required for every map surface (geofence editor,
# shift replay, live guard map). Free tier covers 50k monthly loads.
NEXT_PUBLIC_MAPBOX_TOKEN=
