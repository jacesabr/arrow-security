# Point at PgBouncer in production; direct postgres in dev
DATABASE_URL=postgresql://secureops:secureops@localhost:5433/secureops
REDIS_URL=redis://localhost:6379
SCHEDULER_URL=http://localhost:8080
FIREBASE_SERVICE_ACCOUNT_JSON=
JWT_SECRET=change-me-in-production-minimum-32-chars
PASSWORD_SALT=change-me-too-minimum-16-chars
PORT=4000
LOG_LEVEL=info
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000,http://localhost:5173
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=secureops
# Server-side Mapbox token (separate from NEXT_PUBLIC_/VITE_ tokens used by
# the apps). Used by /accounting to estimate supervisor driving time between
# consecutive shifts via the Mapbox Directions API.
MAPBOX_SERVER_TOKEN=
